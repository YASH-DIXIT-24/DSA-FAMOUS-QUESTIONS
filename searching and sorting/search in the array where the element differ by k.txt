int search(int arr[], int n, int x, int k)
{
    int i=0;
    if(arr[i]==x)
    return i;
    
    while( in && arr[i]!=x)
    {
        i=i+max(1,abs(arr[i]-x)k);
        
        if(in && arr[i]==x)
        return i;

    }
    return -1;
    
}   